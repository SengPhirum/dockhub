{
  "name": "knetrahub-net-api",
  "version": "0.1.0",
  "private": true,
  "description": "KNetraHub-Net-API - standalone server-side API for the KNetraHub-Net subsystem. Independent of the KNetraHub portal: own database schema, own migrations, own auth verification.",
  "type": "module",
  "scripts": {
    "dev": "nitro dev --port 4101",
    "build": "nitro build",
    "start": "node .output/server/index.mjs"
  },
  "dependencies": {
    "jose": "^6.0.11",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "nitropack": "^2.13.4"
  }
}

<script setup lang="ts">
defineProps<{ title: string; subtitle?: string; icon?: string }>()
</script>
<template>
  <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between mb-6">
    <div class="flex items-start gap-3">
      <span v-if="icon" class="mt-0.5 flex size-10 items-center justify-center rounded-xl bg-surface ring-1 ring-hull">
        <UIcon :name="icon" class="size-5 text-beacon" />
      </span>
      <div>
        <h1 class="font-display text-2xl font-semibold tracking-tight text-foam">{{ title }}</h1>
        <p v-if="subtitle" class="mt-0.5 text-sm text-(--color-muted)">{{ subtitle }}</p>
      </div>
    </div>
    <div class="flex flex-wrap items-center justify-start gap-2 sm:justify-end">
      <slot name="actions" />
    </div>
  </div>
</template>
